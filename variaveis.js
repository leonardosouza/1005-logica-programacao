// contexto.instrucao(param1);
console.log("=== VARIAVEIS ===");

// Variaveis
// Espaço reservado na memória para armazenar informações
// Todas as variáveis são prefixadas com as seguintes
// palavras chave: var, let, const

/*
Sintaxe: 

var chave = "valor";
let chave = "valor";
const chave = "valor";
*/

// Tipos Primitivos
var nome = "Leonardo"; // string
console.log("nome", nome, typeof nome);

let idade = 40; // number
console.log("idade", idade, typeof idade);

const alto = true; // boolean
console.log("alto", alto, typeof alto);

var nula = null; // null
console.log("nula", nula, typeof nula);

var indefinida; // undefined
console.log("indefinida", indefinida, typeof indefinida);
